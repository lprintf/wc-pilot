# 客服后台 Demo TODO（2026-09-06）

## 1. 目标

开发一个供客服人员使用的 Web 后台 Demo，客服可以：

- 在用户列表中人工选择目标用户；
- 查看该用户的历史对话记录；
- 输入并发送人工客服消息；
- 明确区分用户消息、AI 消息和人工客服消息；
- 在 Demo 阶段优先打通真实后端数据链路，界面保持简单、可演示。

本任务只覆盖客服内部后台，不包含企业微信管理后台、客服人员复杂权限体系和完整生产级实时通信能力。

## 2. MVP 范围

2026-09-10 已按代码及接口测试同步勾选；第 5 节保留完整端到端验收状态，不将已实现等同于已完成浏览器验收。

### 2.1 页面布局

- [x] 新增客服后台页面，采用三栏布局：用户列表、对话区、用户信息区。
- [x] 用户列表展示昵称、头像、最近消息摘要、最近活跃时间和未读数量；头像加载失败时显示昵称首字。
- [x] 支持按昵称或内部用户 ID 搜索用户。
- [x] 支持点击用户切换当前会话，并在切换后加载历史消息。
- [x] 对话区按时间顺序展示消息，区分发送方和消息来源。
- [x] 对话区支持历史消息分页或滚动加载，避免一次加载全部记录。
- [x] 会话用户信息区展示安全范围内的用户资料，不直接暴露 `external_userid`；客户管理详情另提供鉴权后的显式身份查询。
- [x] 输入框支持发送文本消息，支持 Enter 发送、Shift+Enter 换行。
- [x] 发送中禁用重复提交，并展示成功、失败和重试状态。

### 2.2 后端接口

- [x] `GET /api/admin/users`：返回可供客服选择的用户列表，支持搜索、分页和排序，包含最近消息摘要。
- [x] `GET /api/admin/users/{user_id}/conversation`：返回指定用户的历史消息。
- [x] `POST /api/admin/users/{user_id}/messages`：创建并发送人工客服文本消息。
- [x] `GET /api/admin/users/{user_id}`：返回用户基本资料、备注和标签；会话摘要由列表接口返回。
- [x] 所有后台接口统一返回结构化错误，至少覆盖用户不存在、消息为空、发送失败和未授权。
- [x] 后端通过内部 `user_id` 关联数据，前端发送、编辑和删除仍使用内部 ID。

### 2.3 数据模型

优先复用现有 `app_user`、`user_identity`、`customer_profile` 和会话消息表；如当前消息表无法表达来源，再补充字段或迁移：

```text
conversation_message
  id
  conversation_id
  user_id
  sender_type       # user / ai / human_agent / system
  message_type      # text（MVP）
  content
  source            # wechat_kf / llm / admin
  source_message_id nullable
  send_status       # pending / sent / failed
  occurred_at
  created_at
```

- [x] 为人工客服消息保留发送状态和错误信息，避免网络失败时前端误显示为已发送。
- [x] 为消息增加稳定排序字段，处理同一时间戳下的消息顺序。
- [x] 对用户列表查询增加最近消息和未读数量所需的索引。
- [x] 人工消息先落库 pending，调用企业微信后更新状态；相同 request_id 的重复请求不重复发送。
- [ ] 补齐请求超时但平台实际已投递的重试去重；当前无法保证这种情况下绝不重复投递。

## 3. 鉴权与安全

- [x] 增加最小可用的后台鉴权：使用配置的单一客服账号 HTTP Basic 认证。
- [x] 后端接口必须在服务端校验客服身份，不能仅依赖前端隐藏页面。
- [x] 限制客服可访问的数据范围，避免通过修改 URL 查看非授权用户。
- [ ] 日志中不得输出消息全文、密钥、access token、Session 或完整外部用户标识。
- [x] 消息内容进行长度限制和基础输入校验；前端使用 React 文本渲染。
- [x] 发送人工消息时记录操作人和操作时间，便于后续审计扩展。

## 4. 实现顺序

- [x] 1. 盘点现有路由、消息表和企业微信客服发送封装，确定复用边界。
- [x] 2. 实现用户列表和历史消息只读接口。
- [x] 3. 创建客服后台页面，完成三栏交互。
- [ ] 4. 页面已接入真实接口；继续补齐空历史记录、发送失败后的即时列表刷新等状态。
- [x] 5. 接入人工客服消息发送，处理请求幂等、失败重试和消息回显。
- [x] 6. 增加鉴权、审计字段、敏感信息脱敏和基础安全校验。
- [ ] 7. 使用本地或测试环境完成端到端验收。

## 5. MVP 验收标准

以下为浏览器与实际投递的联合验收清单；接口测试和构建通过的记录见实施记录。

- [ ] 客服打开后台后能够看到用户列表，并能搜索和人工选择用户。
- [ ] 选择用户后能够看到该用户的历史对话，消息发送方和时间展示正确。
- [ ] 客服发送文本后，页面立即显示明确的发送中状态，并在成功后显示已发送。
- [ ] 企业微信发送失败时，页面显示失败原因或可重试入口，数据库不会产生错误的已发送记录。
- [ ] 刷新页面或重新选择用户后，已发送人工消息仍能从后端正确加载。
- [ ] 空用户列表、空历史记录、用户不存在、接口超时和发送失败均有可理解的提示。
- [ ] 普通用户无法通过接口或修改参数访问客服后台数据。
- [ ] 不影响现有 AI 自动回复和微信客服消息同步流程。

## 6. 暂不纳入 MVP

- [ ] 多客服并发抢单、转接、排队和会话分配。
- [ ] 客服账号、角色、组织架构和细粒度 RBAC。
- [ ] 图片、文件、语音、视频、富文本和快捷回复。
- [ ] WebSocket 实时推送；第一版可采用手动刷新或短轮询。
- [ ] 完整统计报表、满意度评价和会话标签体系。
- [ ] 企业微信官方“会话内容存档”数据源接入；仍沿用当前客服消息数据边界。

## 7. 相关代码与文档

- `docs/ARCHITECTURE.md`
- `docs/TODO/260904.md`
- `backend/main.py`
- `backend/README.md`
- `backend/wechat_bot/replies.py`
- `backend/scripts/test_wechat_kf_api.py`
- `compose.dev.yaml`
- `docs/COMPOSE_DEV.md`
- `docs/WECHAT_KF_SETUP.md`
- `docs/wechat-api.md`

## 实施记录（2026-09-06）

- 已完成三栏客服后台页面 `/admin`，支持用户搜索、分页列表、未读数、会话切换、历史消息分页和人工文本发送。
- 已新增 `/api/admin/users`、`/api/admin/users/{user_id}`、`/conversation` 和 `/messages` 接口；接口统一返回结构化错误，并在服务端按客服账号校验数据范围。
- 已新增统一 `conversation_message` 消息流水和 `admin_user_state` 已读状态表；旧 `processed_message` 数据启动时自动回填，人工消息保留 pending/sent/failed 状态、操作员、时间及重试标识。
- 已加入配置式 HTTP Basic 最小鉴权（`ADMIN_USERNAME` / `ADMIN_PASSWORD`）、输入长度校验、HTML/日志脱敏、请求幂等和发送失败重试。
- 尚未纳入 MVP 的 WebSocket、复杂 RBAC、多媒体消息、会话存档和生产级二次验证仍按原范围保留。

## 实施记录（2026-09-10）

### 回复窗口与发送诊断

- [x] 自动回复和人工回复统一按客户最后发消息后的 48 小时、最多 5 条管理额度；每条回复末尾附加发送本条后的剩余次数、剩余时长和客户回复可重置的提示。
- [x] 额度按客服账号和客户持久化，AI 与人工共用；非文本客户消息同样重置额度，重复回调及更早消息不重置，发送失败不扣减。
- [x] 同一批拉取消息先登记后发送，共用额度；同进程并发发送串行检查，避免超发。超限自动回复保留客户文本并推进游标。
- [x] 提示预留 UTF-8 字节空间，消息总长不超过 2048 字节；提示不写入 LLM 历史，登录链接仍在历史记录中隐藏。
- [x] 窗口过期或次数用尽返回 `409`、`reply_window_expired` / `reply_limit_reached` 和明确原因；其他发送故障保留 `502`。
- [x] 人工发送被拦截时打印 `message_id`、业务错误码、`source` 和 `errcode`。本地拦截记录 `source=local errcode=None`；企业微信限制记录 `source=wecom` 及真实错误码 `95001` / `95002`，不将本地判断冒充平台返回。
- [x] 初始化失败按 5、10、20、40、60 秒退避重试，保留已有游标；接口错误码出现在脱敏日志和 readiness 结果中，成功后自动恢复。

### 测试脚本与开发环境

- [x] 非交互脚本新增 `--list-customers`，按最近消息列出客户昵称、完整 `external_userid`、性别及最后消息时间；资料接口失败时仍输出 ID 和时间。
- [x] 新增 `--external-userid` 指定测试对象，直接请求企业微信发送接口并打印真实 `errcode` / `errmsg`，不受后端本地额度检查影响。只有显式 `--send-test` 才发送消息。
- [x] 新增 `--database` 只读查询本地保存的全部历史客户，展示后台数字 ID 和企业微信 ID 的对应关系；支持按 `--open-kfid` 筛选。企业微信 `sync_msg` 的最近三天范围不能通过增加 `--max-pages` 扩大。
- [x] 新增显式开发覆盖 `compose.dev.yaml`，挂载后端源码、脚本和测试，启用 Uvicorn 自动重载；保留镜像内 Linux 依赖、非 root 用户和现有数据卷。
- [x] 挂载前端源码和 Nginx 配置；前端保持一次性构建，修改源码后无需重建镜像，但需要重新执行构建服务。
- [x] 更新开发文档及脚本说明，容器命令使用相对路径，避免 Git Bash 将 `/app/...` 转为 Windows 路径。

### 验证与后续事项

- [x] 开发容器内执行 `python -m unittest discover -s tests`：66 项测试通过，涵盖额度、错误日志、初始化恢复、脚本目标选择和数据库查询。
- [x] 开发 Compose 合并配置检查通过，前端构建成功，Nginx 配置检查通过，后端和网关健康。
- [x] 实际验证目录挂载触发自动重载，网关 `/health/ready` 返回 ready；数据库只读查询成功列出已保存的 2 位客户。
- [ ] 补充浏览器端人工发送、失败展示及重试的完整验收；本次未通过脚本向真实客户发送测试消息。
- [ ] 如需多进程部署，增加跨进程共享发送锁；当前额度锁适用于单进程开发部署。
- [ ] 完善服务外发送及超时后实际投递成功时的额度校准；脚本或其他工具发送不会更新本服务计数，目前以企业微信最终限制结果为准。

## 客户管理补充（2026-09-10）

- [x] 新增独立「客户管理」入口，支持客户总数、昵称/内部 ID 搜索、排序、20 条分页、刷新、详情和进入会话。
- [x] 详情提供企业微信 ID 的显式查询及复制；新身份接口校验管理员和客服账号范围，设置 `Cache-Control: no-store`。
- [x] 支持本地昵称、性别编辑，客户备注与标签；与微信资料缓存分开存储，微信同步不会覆盖人工编辑，记录编辑操作员和时间。
- [x] 支持删除客户，页面输入内部 ID 二次确认，接口验证 CSRF 和确认 ID；删除正文、客户资料、管理信息及登录凭据，保留无正文去重和额度记录。
- [x] 删除拒绝存在 pending 消息或跨账号关联的客户，与自动回复使用同一客户锁；测试验证会话级联删除、保留其他客户及旧消息不会重新回复。
- [x] 会话列表显示真实头像并提供加载失败回退；切换客户时重置输入框，避免将旧客户草稿带到新会话。
- [x] TypeScript 类型检查、前端构建通过，新静态产物已更新到开发环境。
- [x] 客户管理加入后，完整后端测试共 73 项通过，涵盖编辑持久化、微信同步隔离、权限、CSRF、身份查询、删除确认及级联清理。
- [ ] 浏览器可视化与交互验收：当前浏览器工具返回 `unsupported Codex auth method: apikey`，尚未完成此项；没有对真实客户执行编辑、删除或发送测试。

## 连续回复排查（2026-09-10）

- [x] 查明 18:20 的 5 条显示包含 3 次真实投递和 2 条历史回填副本；三次投递分别回答了积压的旧消息及最新消息，LLM 均返回 HTTP 403 后使用兜底回复。
- [x] 持久化真实回复 ID，历史回填复用该 ID；修复重复 `mark_sent` 仍插入消息的问题。升级只删除能唯一匹配真实投递的合成副本，保留真实投递记录。
- [x] 同批同客户只发一条回复；后续改为将全部待回复文本按时间合并给 LLM，详见下面的上下文合并记录。
- [x] 人工发送本地判定过期或超额时，先同步目标客户遗漏的消息并核对已发送次数，再决定是否允许发送；其他客户的回调游标和回复流程不被跳过。
- [x] 修改前已备份开发数据库，修复后本次事件显示为 3 条真实记录、0 条合成副本，外键检查正常，服务 ready。
- [ ] LLM 接口 HTTP 403 仍待排查上游访问权限；本次没有修改密钥或发出真实测试消息。

## LLM 上下文合并（2026-09-10）

- [x] 同次拉取中同一客户的全部待回复问题按时间戳合并为一轮输入，不再只将最新文本作为当前提问。
- [x] 历史上下文保留最近 12 条客户、AI、人工回复并附 UTC 时间戳；当前批次问题不受该条数上限截断，不重复混入历史。
- [x] 提示词要求综合回答所有问题、理解后发修正、避免重复回答已解决问题。登录链接命令继续走专用流程。
- [x] 一批只发送一条综合回复，逐条原文与单条回复在同一事务落库；测试覆盖多问题、时间排序、跨客户隔离、重试完整性、重复回调及事务回滚。
- [ ] 跨回调的短暂等待合并窗口尚未添加；LLM 请求期间到达的新消息由下一轮处理。
